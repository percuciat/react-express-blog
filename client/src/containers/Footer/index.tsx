import React from 'react';

const Footer = (props: any) => {
    return (
        <>
            <footer>
                footer
            </footer>
        </>
    );
};

export default Footer;